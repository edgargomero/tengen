# Adaptaciones de upstream — log de cambios, re-sync y cómo retomar

**Propósito.** tengen **adapta** código de terceros (sobre todo web-katrain, MIT) en lugar de reimplementarlo. Ese código sigue evolucionando: saldrán releases nuevas de web-katrain, KataGo (formato de red / encoding) y onnxruntime-web. Este documento existe para que **cualquier LLM (o persona) pueda (a) retomar el proyecto sin contexto previo y (b) re-aplicar nuestras adaptaciones a una versión nueva de upstream** de forma mecánica y verificable. **Es de mantenimiento obligatorio**: toda tarea que vendorice o adapte un archivo de terceros debe añadir/actualizar su entrada aquí en el mismo commit (constraint global del plan).

Regla de oro: **mantener el diff contra upstream mínimo y localizado**, y que el gate de correctitud (`kata-raw-nn`, ver `decisiones-adaptacion.md`) sea la red de seguridad tras cualquier re-sync.

---

## Referencias upstream fijadas (pins)

| Dependencia | Versión/commit fijado | Ubicación local | Rol |
|---|---|---|---|
| **web-katrain** (Sir-Teo/web-katrain, MIT) | commit `7a0a487` (2026-07-02) | `~/dev/vendor/web-katrain` | Fuente de adaptación del board+encoding+MCTS |
| **KataGo** (lightvector/KataGo, MIT) | desktop `1.16.5` (brew); red `kata1-b18c384nbt-s9996604416`; `humanv0` de release `v1.15.0` | binario por `brew`; `.bin.gz` en `packages/engine/models/katago-bin/` (gitignored) | Genera vectores de referencia `kata-raw-nn`; define el encoding V7 y el contrato I/O |
| **katago-onnx** (kaya-go/katago-onnx, AGPL — solo herramienta local) | clon en `~/dev/vendor/katago-onnx` (pixi) | idem | Convierte `.bin.gz`→`.onnx` (uso local, no se vendoriza) |
| **onnxruntime-web** | `^1.24.3` (los tensores `float16` = `Uint16Array`) | npm | Runtime de inferencia; **no** subir de versión sin re-medir la fase 0 |

Cuando cualquiera de estos publique una versión nueva, seguir el **runbook de re-sync** (abajo). El watcher de releases upstream (ver `CLAUDE.md` → "Monitoreo de releases upstream") debe avisar de estos repos.

---

## Log de adaptaciones por archivo

Cada archivo vendorizado en `packages/engine/src/vendor/web-katrain/` proviene de `~/dev/vendor/web-katrain/src/engine/katago/<mismo nombre>` en el commit fijado. La columna **Cambios de tengen** es lo que hay que **re-aplicar** sobre una versión nueva de upstream.

| Archivo | Origen (web-katrain@7a0a487) | Cambios de tengen | Task | Notas de re-sync |
|---|---|---|---|---|
| `fastBoard.ts` | `src/engine/katago/fastBoard.ts` | (Task 1) verbatim + cabecera. (Task 3) **sin cambios de código**: a diferencia de `evalV8.ts`/`analyzeMcts.ts`, el `fastBoard.ts` de upstream ya es autocontenido (no importa de `../../types`; `StoneColor` se define localmente en el propio archivo), así que no había imports que arreglar — el diff contra upstream sigue siendo solo la cabecera. Task 3 validó `setBoardSize`/`playMove`/`undoMove`/`PASS_MOVE` contra `@sabaki/go-board` como oráculo (200 jugadas aleatorias con PRNG `mulberry32` sembrado + caso `PASS_MOVE`) en `tests/board.test.ts`. | 1, 3 | Board+escaleras+Benson. Diff mínimo esperado; si upstream cambia firmas de `computeLadderFeaturesV7KataGo`/`computeAreaMapV7KataGo`/`playMove`, actualizar los llamadores en `encoding/featuresV7.ts` y `search/mcts.ts`. Si una versión nueva de upstream SÍ introduce un import a `../../types` en este archivo, reemplazarlo por tipos locales (mismo patrón pendiente en `evalV8.ts`/`analyzeMcts.ts`) sin tocar la lógica. **Landmine conocido:** `playMove` **muta antes de lanzar** en la rama de suicidio (no en las de ocupado/ko simple) — ver `decisiones-adaptacion.md §7`. Si un re-sync toca la lógica de `playMove`, re-verificar ese comportamiento y grepear el string exacto `'Illegal suicide move'` (del que dependen el rollback y el test de Task 3). |
| `featuresV7Fast.ts` | `src/engine/katago/featuresV7Fast.ts` | (Task 5) vendorizado **solo como oráculo de test** (NHWC intacto), verbatim EXCEPTO: (1) imports resueltos a defs locales (`Player`/`GameRules` de `../../types`, `getOpponent` de `../../utils/gameLogic`) — mismo patrón que otros vendorizados; (2) dos `!` en índices de tupla (`passGlobals[i]!`, `historyPlanes[i]!`) exigidos por `noUncheckedIndexedAccess` (cambio de tipo, no de lógica). El encoder de producción es el fork NCHW `encoding/featuresV7.ts`. | 5 | Si upstream cambia planos/globals, se detecta porque el test diferencial NCHW↔NHWC (`tests/featuresV7.test.ts`) y el de `kata-raw-nn` fallan. Re-aplicar el cambio de layout en el fork. Al re-copiar de upstream, re-aplicar (1) y (2). |
| `encoding/featuresV7.ts` | `src/engine/katago/featuresV7Fast.ts` (`fillInputsV7Fast`) | (Task 5) **FORK** del encoder V7 con cabecera de origen. Cambios sobre el original: (a) indexador espacial → NCHW `c·N²+y·N+x`; (b) `BOARD_SIZE`/361 → `N=state.boardSize`; (c) los mapas de libertades/área/escaleras se precomputan dentro del fork llamando a `fastBoard` (en el oráculo llegan por argumento). Toda la lógica de planos/globals/reglas/onda-de-komi es idéntica. Convención propia: koPoint=−1 para las escaleras de `prevStones`/`prevPrevStones` (planos 15/16), y prev/prevPrev al inicio de partida = tablero inicial (no repite el actual como el fallback de web-katrain). | 5 | Es un fork, no un re-copy: cuando upstream toque `fillInputsV7Fast`, portar el delta de lógica manteniendo (a)/(b)/(c). Gate: `tests/featuresV7.test.ts` (diferencial) + `kata-raw-nn` (Task 10, único que caza divergencias de flags de área o de koPoint histórico vs KataGo real). |
| `scoreValue.ts` | `src/engine/katago/scoreValue.ts` | (Task 7) **vendorizado, acoplamiento a `fastBoard.ts` roto.** Se quitó `import { BOARD_AREA, BOARD_SIZE } from './fastBoard'`. `initScoreValueTables()` → `initScoreValueTables(boardSize: number)`: ya no lee ningún global; solo recalcula la tabla si el `boardSize` recibido difiere del último cacheado (`svTableBoardSize`), que ahora queda keyeado 100% por el parámetro (cache de una sola entrada, se recomputa al cambiar de tamaño — no hace falta multi-entrada, ver `decisiones-adaptacion.md`). `expectedWhiteScoreValue(args)` gana un campo `boardSize: number` (además de `sqrtBoardArea`, que ya era parámetro en upstream) y llama a `initScoreValueTables(args.boardSize)`. `getSqrtBoardArea()` → `getSqrtBoardArea(boardArea: number)`, misma fórmula `Math.sqrt(boardArea)`, ya no lee `BOARD_AREA`. Ninguna fórmula numérica cambió. tsc: 0→0 errores (ya estaba limpio). | 7 | Cambio localizado en la firma de `expectedWhiteScoreValue`/`initScoreValueTables`/`getSqrtBoardArea`. `analyzeMcts.ts` (aún no adaptado, ver su fila) se tocó al mínimo para seguir compilando con la firma nueva: sus 4 llamadas ahora pasan explícitamente `BOARD_SIZE`/`BOARD_AREA` (el mismo global de `fastBoard.ts` que ya usaban para todo lo demás, solo que ahora se lo pasa el propio caller en vez de que `scoreValue.ts` lo leyera internamente) — Task 8 debe reemplazar ese passthrough por el board size real del Worker al hacer la costura del `NNEvaluator`. |
| `evalV8.ts` | `src/engine/katago/evalV8.ts` | (Task 7) **vendorizado, imports arreglados.** `import type { Player } from '../../types'` (el módulo no exporta `Player`) → `type Player = 'black' \| 'white'` local, mismo patrón que `featuresV7Fast.ts`/`gameState.ts`/`encoding/featuresV7.ts`. Se añadieron 6 `!` de non-null assertion en los accesos a `valueLogits[i]`/`scoreValue[i]` (`ArrayLike<number>`) exigidos por `noUncheckedIndexedAccess`. Sin tocar la matemática de `postprocessKataGoV8` (softmax/softplus/multiplicadores/perspectiva). tsc: 7→0 errores. | 7 | Si upstream cambia el orden de `scoreValue[]` o los multiplicadores, actualizar aquí y en el `NNEvaluator`. Campos de retorno reales (verificados contra el código): `blackWinProb`, `blackScoreLead`, `blackScoreMean`, `blackScoreStdev`, `blackNoResultProb`. |
| `searchParams.ts` | `src/engine/katago/searchParams.ts` | (Task 1) verbatim + cabecera. | 1 | Constantes de búsqueda; diff nulo esperado. |
| `apps/web/src/models/progress.ts` | `src/utils/downloadProgress.ts` | (Fase 1, Task 1) **solo la matemática de progreso**, verbatim: `getContentLength`/`getProgressPercent`. El loop de streaming de upstream (`getReader()` + acumulación en un `Blob` en memoria, `responseBlobWithProgress`/`fetchBlobWithProgress`) **NO se porta** — tengen reimplementa el loop en `apps/web/src/models/modelCache.ts` (Fase 1, Task 2) con escritura incremental a OPFS (RAM plana, sin materializar el ONNX completo en memoria). No vive en `packages/engine/src/vendor/web-katrain/` como el resto de esta tabla (ese directorio es del motor); vive en `apps/web/src/models/` y su atribución está en `apps/web/THIRD-PARTY-LICENSES` (documento separado del de `packages/engine`). | Fase 1 / Task 1 | Diff nulo esperado: si upstream cambia la fórmula de `getContentLength`/`getProgressPercent`, re-portar verbatim. Gate: `apps/web/tests/progress.test.ts` (Node). |
| `apps/web/src/analysis/vendor/web-katrain/types.ts` | `src/types.ts` | (Fase 3a, Task 3) **subconjunto TRIMMED, no un port del archivo.** El `types.ts` completo de upstream (175+ líneas: `GameSettings` con ~70 campos de UI/config, `BoardState`, `RegionOfInterest`, temas de tablero) es mucho más grande que lo que Task 3 necesita. Se define solo lo que los 5 archivos portados de este directorio leen (verificado campo-por-campo leyendo su código fuente, no por inspección superficial): `Player` (`'black' \| 'white'`), `Move {x,y,player}`, `CandidateMove {x,y,winRate,scoreLead,visits,pointsLost,order,prior?}`, `AnalysisResult {rootWinRate,rootScoreLead,rootVisits?,moves}`, `GameNode {move,parent,analysis?}`. Campos del original que SÍ existen pero NINGUNO de los 5 archivos lee (`winRateLost`/`scoreSelfplay`/`scoreStdev`/`relativePointsLost`/`pv`/`ownership` de `CandidateMove`; `rootScoreSelfplay`/`rootScoreStdev`/`territory`/`policy`/`ownershipStdev`/`ownershipMode` de `AnalysisResult`) quedaron fuera deliberadamente (YAGNI) — Task 4 puede extender este archivo si `gameReport.ts`/`playedMoveQuality.ts` los necesita. **(Fase 3a, Task 4) `CandidateMove.pv?: string[]` AÑADIDO.** El brief de Task 4 afirmaba ("verificado") que ni `CandidateMove` ni `GameNode` necesitaban campos nuevos para portar `gameReport.ts`/`playedMoveQuality.ts` — esa afirmación era incorrecta: `gameReport.ts` (verbatim, línea `pv: top?.pv,` dentro de la construcción de `MoveReportEntry`) SÍ lee `.pv` de `CandidateMove`. Detectado por `tsc --noEmit` (no por los tests: Vitest/esbuild no chequea tipos, y `top?.pv` es runtime-safe con o sin el campo — el error era 100% de tipos, cero de comportamiento). Se añadió el campo (opcional, aditivo, no rompe ningún literal de Task 3) exactamente como esta fila ya preveía. `GameNode` NO ganó campos en `types.ts`: `getNextMoveQuality` (`playedMoveQuality.ts`) sí necesita `children`, pero se tipó LOCALMENTE en ese archivo (`GameNodeWithChildren = GameNode & {children: GameNode[]}`) en vez de extender el tipo compartido, porque ninguna otra función portada lee `children` y el campo no fluye a través de `AnalysisResult` (a diferencia de `pv`, que si fluye por `AnalysisResult.moves` y por eso sí necesitaba ir en el tipo compartido). | 3, 4 | No es un archivo 1:1 con upstream — no hay "diff contra upstream" mecánico posible. Si una tarea futura necesita más campos de `CandidateMove`/`AnalysisResult`, añadirlos aquí mismo (extender, no reemplazar), correr `tsc --noEmit` (el gate real que caza estos gaps, no los tests) y documentar el campo nuevo en esta fila. Gate: `apps/web/tests/nodeAnalysis.test.ts`/`analysisCoverage.test.ts`/`gameReport.test.ts`/`playedMoveQuality.test.ts` (uso indirecto vía los tipos) + `npx -w apps/web tsc --noEmit`. |
| `apps/web/src/analysis/vendor/web-katrain/nodeAnalysis.ts` | `src/utils/nodeAnalysis.ts` | (Fase 3a, Task 3) **verbatim salvo el import de tipos** (`from '../types'` → `from './types'`, apuntando al `types.ts` TRIMMED de este mismo directorio). Sin cambios de lógica: `computeNodePointsLost` (con su fallback a `parent.analysis.moves.find(...)` cuando no hay `rootScoreLead` válido en padre/hijo) y `getEvaluationClass` (clamp sobre `DEFAULT_EVAL_THRESHOLDS = [12,6,3,1.5,0.5,0]`) idénticos. `values[index-1]`/accesos por índice no requirieron `!` nuevos para `noUncheckedIndexedAccess` (el propio código fuente ya guarda con `typeof`). tsc: 0→0 errores. | 3 | Diff mínimo esperado (solo el import). Gate: `apps/web/tests/nodeAnalysis.test.ts` (19 casos: raíz, fallback con/sin candidato, signo por color, bordes de umbral). |
| `apps/web/src/analysis/vendor/web-katrain/analysisSummary.ts` | `src/utils/analysisSummary.ts` **+** `src/utils/manualScore.ts` (solo `formatResultScoreLead`) | (Fase 3a, Task 3) **dos orígenes fusionados en un archivo, decisión de adaptación explícita.** `analysisSummary.ts` de upstream importa `formatResultScoreLead` desde `manualScore.ts` (~100 líneas sobre puntaje manual desde ownership/Japanese scoring — tengen no usa esa vía: `Analysis.ownership` es siempre `undefined` hoy, hallazgo 1 del plan de Fase 3a). `manualScore.ts` completo **NO se vendoriza** (fuera de alcance, código no relacionado); se porta solo esa función (8 líneas, autocontenida) inline en este archivo, exportada por consistencia con el resto. `formatAnalysisWinRate`/`formatAnalysisScoreLead`/`summarizePointsLost` verbatim. tsc: 0→0 errores. | 3 | Si upstream cambia `formatResultScoreLead` en `manualScore.ts`, hay que ir a buscarlo ahí explícitamente — un `diff` automático contra `analysisSummary.ts` de upstream NO lo detectaría (está en otro archivo). Gate: `apps/web/tests/analysisSummary.test.ts` (23 casos: cada rama de cada función, `Jigo`/`-0`, umbrales `0.05`/`0`/`1` de `summarizePointsLost`). |
| `apps/web/src/analysis/vendor/web-katrain/analysisCoverage.ts` | `src/utils/analysisCoverage.ts` | (Fase 3a, Task 3) **verbatim salvo el import de tipos.** Verificación del hallazgo 3 del plan de Fase 3a (¿depende de `branchNavigation.ts`?): **confirmado que NO** — su único import en upstream es `type { GameNode } from '../types'`; no hay ninguna noción de "rama activa" involucrada. `isReportReadyAnalysis`/`summarizeAnalysisCoverage` (con su `Pick<GameNode,'analysis'>` y el `isAnalyzed` override) idénticos. tsc: 0→0 errores. | 3 | Diff mínimo esperado (solo el import). `isReportReadyAnalysis` es el motivo por el que `gameReport.ts` (Task 4) depende de este archivo. Gate: `apps/web/tests/analysisCoverage.test.ts` (13 casos: cobertura 0/parcial/completa, `isReportReadyAnalysis` con/sin `moves`/`rootVisits`, override de `isAnalyzed`). |
| `apps/web/src/analysis/vendor/web-katrain/analysisSmoothing.ts` | `src/utils/analysisSmoothing.ts` | (Fase 3a, Task 3) **verbatim**, sin dependencias externas ni de tipos. `smoothAnalysisGraphValues` (promedio con el valor previo, guardado por `Number.isFinite`) idéntico. tsc: 0→0 errores. | 3 | Diff nulo esperado. Gate: `apps/web/tests/analysisSmoothing.test.ts` (7 casos: primer valor sin cambios, promedio simple, `NaN` en valor/previous sin promediar). |
| `apps/web/src/analysis/vendor/web-katrain/topMoveMetric.ts` | `src/utils/topMoveMetric.ts` | (Fase 3a, Task 3) **imports resueltos a defs locales** (hallazgo 18 del plan, mismo patrón que `evalV8.ts`/`featuresV7Fast.ts` del motor). `TopMoveMetric`/`PolicyHeatmapMetric` en upstream son `GameSettings['trainerTopMovesShow']`/`GameSettings['analysisPolicyMetric']`; tengen no tiene `GameSettings`, así que se reescribieron como uniones literales locales con los valores exactos del vendor (`'top_move_score' \| 'top_move_delta_score' \| 'top_move_winrate' \| 'top_move_delta_winrate' \| 'top_move_visits' \| 'top_move_nothing'` y `'policy' \| 'delta_score' \| 'delta_winrate'`). Resto (las 4 listas de opciones + `getTopMoveMetricLabel`/`nextTopMoveMetric`/`getPolicyHeatmapMetricLabel`/`nextPolicyHeatmapMetric`, todas con fallback `?? OPTIONS[0]!` ante métrica desconocida) verbatim. No importa nada de `types.ts` de este directorio — independiente. Aún no cableado a nada (lo consumen Tasks 8/10). tsc: 0→0 errores. | 3 | Si upstream añade/quita un valor de `trainerTopMovesShow`/`analysisPolicyMetric` en `GameSettings`, replicar el cambio en las dos uniones literales locales. Gate: `apps/web/tests/topMoveMetric.test.ts` (16 casos: tamaño de las 4 listas, labels long/short, wrap-around de `next*`, fallback a la opción 0 con valor desconocido/`undefined`). |
| `apps/web/src/analysis/vendor/web-katrain/gameReport.ts` | `src/utils/gameReport.ts` | (Fase 3a, Task 4) **archivo COMPLETO portado** (incluye `getReportStudyFocus`/`getReportRecoveries`/`describeReportSwing`, aún no consumidos por la UI de 3a — decisión de alcance ya tomada por el plan, coherente con "adaptar archivos enteros" de `CLAUDE.md`). **Cambio de firma de `computeGameReport`:** `{currentNode: GameNode; thresholds; activeBranchChildIds?: ActiveBranchMap; depthFilter?; phaseFilter?}` → `{mainLine: GameNode[]; boardSize: number; thresholds; depthFilter?; phaseFilter?}`. `mainLine` es la salida DIRECTA de `GameTree.mainLine()` de tengen (ya excluye la raíz, `apps/web/src/game/gameTree.ts:124-132`) tras adaptar cada nodo a `{move,parent,analysis?}` — el `.parent` del primer elemento debe apuntar a un nodo que represente la raíz de tengen (con su `.analysis` si corresponde), o la primera jugada se omite del reporte silenciosamente (comportamiento ya existente del código, verificado con test dedicado, no un bug introducido). Dentro del loop: `boardSize` viene de `args.boardSize` (reemplaza `currentNode.gameState.board.length`) y `moveNumber = depth + 1` (reemplaza `n.gameState.moveHistory.length`, aprovechando que `mainLine` ya no tiene raíz — el índice 0-based del array es "jugada depth+1"). `nodesForCurrentBranch`/el import de `branchNavigation.ts` se eliminan enteros (ya no hace falta resolver "rama activa": `mainLine` ya ES la secuencia lineal). Resto (histograma, stats por jugador, `classifyMoveByRankAndPolicy`, `sortMoveReportEntries`, `getReportTurningPoints`, `getPhaseThresholds`/`getMovePhase`, etc.) **verbatim**, incluyendo `computePointsLostStrict` (privada, NO reutiliza `computeNodePointsLost` de `nodeAnalysis.ts` — así era también en upstream, son funciones con contratos de "readiness" distintos). **`classifyMoveByRankAndPolicy` — discrepancia de prosa detectada (no de código):** el brief de Task 4 describía el comportamiento de desacuerdo rank×prior como "toma el peor veredicto"; leyendo el código real (`Math.min(rankIndex, priorIndex)` sobre `MOVE_POLICY_CATEGORIES` ordenado mejor→peor), en realidad toma la categoría MENOS severa de las dos (el "mejor" veredicto de las dos lecturas). Se portó verbatim (fidelidad del port > prosa del brief) y se documentó en la cabecera del archivo y en el test (`gameReport.test.ts`, caso "CRUCE en desacuerdo"). **Exclusión deliberada de `branchNavigation.ts`/`positionEval.ts`:** ver fila de `playedMoveQuality.ts` (mismo motivo, documentado una sola vez). tsc: 1 error nuevo (`CandidateMove.pv` faltante, ver fila de `types.ts`) → 0 tras el fix. | 4 | Si upstream toca `computeGameReport`, re-aplicar el cambio de firma (`mainLine`+`boardSize` en vez de `currentNode`+`activeBranchChildIds`) y el `moveNumber = depth+1`. El resto del archivo es diff mecánico (mismo patrón que `nodeAnalysis.ts`: solo el import de tipos). Gate: `apps/web/tests/gameReport.test.ts` (20 casos: off-by-one de `moveNumber` con secuencia de 4 jugadas, signo de `pointsLost` para ambos colores, histograma/stats, nodo sin `.analysis` propia que se omite sin lanzar mientras el resto de la secuencia sigue procesándose, `mainLine[0].parent` con/sin `.analysis`, las 5 categorías + el cruce en desacuerdo de `classifyMoveByRankAndPolicy`, ambos criterios de `sortMoveReportEntries`, corte de `getReportTurningPoints`, bordes de `getPhaseThresholds`/`getMovePhase`/`getPointLossBucket`). |
| `apps/web/src/analysis/vendor/web-katrain/analysisQueue.ts` | `src/utils/analysisQueue.ts` | (Fase 3a, Task 6) **verbatim, autocontenido.** No importa ningún tipo de web-katrain (`GameNode`/`CandidateMove`/etc.), solo tipos genéricos (`<T>`) — la clase `AnalysisQueue` (prioridad/grupo/`preempt`/`staleKey`/cache) y las clases de error `AnalysisQueueCanceledError`/`AnalysisQueueStaleError` (+ sus type guards) portadas sin cambios de lógica. **ÚNICA adaptación deliberada:** se omite la última línea del original, `export const analysisQueue = new AnalysisQueue();` (singleton de módulo) — tengen evita estado global no-scoped a una sesión (mismo principio que `EngineManager`). El consumidor nativo `apps/web/src/analysis/reviewScheduler.ts` (NO portado, sin atribución — solo consume esta clase) instancia su PROPIA `AnalysisQueue` por sesión de Analizar, adaptando `EngineManager.analyze` (streaming, Task 2) a la forma `(ctx) => Promise<Analysis>` que un job espera. Diff verificado byte-a-byte contra el vendor (excepto la línea omitida). tsc: 0→0 errores. | 6 | Diff mínimo esperado (mecánico, la clase no tiene dependencias). Si upstream cambia `analysisQueue.ts`, re-portar verbatim y re-omitir la misma línea del singleton. `reviewScheduler.ts` es el único lugar que depende de la mecánica exacta de `preempt`/`staleKey`/`AnalysisQueueSignal.abort` (síncrono) — si upstream cambia esa mecánica, revisar los 4 caminos de asentamiento de `runAnalyzeJob` (éxito/error/abort/timeout) contra el nuevo comportamiento. Gate: `apps/web/tests/analysisQueue.test.ts` (prioridad+orden, preempt, staleKey solo-pendientes, cancelGroup) + `apps/web/tests/reviewScheduler.test.ts` (los 4 caminos de asentamiento + el test de settle-on-abort que prueba que un job preemptado no atasca `pump()` — hallazgo 21 del plan de Fase 3a). |
| `apps/web/src/analysis/vendor/web-katrain/gameAnalysisProgress.ts` | `src/utils/gameAnalysisProgress.ts` | (Fase 3a, Task 7) **verbatim, sin cambios.** Archivo autocontenido (58 líneas, sin dependencias externas ni de tipos de web-katrain): `formatDuration` (s/m/h con y sin resto) y `summarizeGameAnalysisProgress` (`done`/`total`/`startedAtMs`/`nowMs` SIEMPRE parámetros explícitos, nunca `Date.now()` interno) idénticos. Consumido por `apps/web/src/analysis/gameReview.ts` (NATIVO, no portado, sin atribución — solo consume esta función a través de `GameReview.progress(nowMs)`, que sigue el mismo estilo de `nowMs` explícito). tsc: 0→0 errores. | 7 | Diff nulo esperado (archivo autocontenido, sin puntos de fricción con el resto del código portado). Si upstream cambia la fórmula de ETA o los formatos de `formatDuration`, re-portar verbatim. Gate: `apps/web/tests/gameAnalysisProgress.test.ts` (9 casos: los 3 formatos de duración con/sin resto, `total<=0`→null, clamp de `done`, condición exacta de ETA, labels exactos). |
| `apps/web/src/analysis/vendor/web-katrain/playedMoveQuality.ts` | `src/utils/playedMoveQuality.ts` | (Fase 3a, Task 4) **dos cambios de firma puntuales.** (1) `getPlayedMoveQuality(node, pointsLostOverride?)` leía `node.gameState.board.length` para el tamaño de tablero; el `GameNode` trimmed no tiene `gameState`, así que gana un parámetro explícito: `getPlayedMoveQuality(node, boardSize: number, pointsLostOverride?)`, pasado directo a `formatBoardMoveLabel(move, boardSize)`. `formatBoardMoveLabel` en sí NO cambia (sigue con su default `boardSize=19`). (2) `getNextMoveQuality(node, activeBranches?)` usaba `getActiveChild(node, activeBranches)` de `branchNavigation.ts` (excluido); como Fase 3a es main-line-only, "el hijo activo" es siempre `node.children[0] ?? null` (mismo criterio que `GameTree.mainLine()` de tengen, que navega estrictamente por `children[0]`) → firma adaptada `getNextMoveQuality(node, boardSize): PlayedMoveQuality \| null`, sin `activeBranches`. **`children` se tipa LOCALMENTE** (`export type GameNodeWithChildren = GameNode & {children: GameNode[]}`, exportado desde este archivo) en vez de extender `types.ts` — ninguna otra función portada de este directorio necesita `children`, y el `GameNode` real de tengen (`apps/web/src/game/gameTree.ts:31-36`) ya lo trae nativamente, así que Task 5 puede satisfacer este parámetro sin fricción. Resto (`formatBoardMoveLabel`, `candidateRank`, `findBestCandidate`, la lógica interna de `getPlayedMoveQuality`) **verbatim**. **Exclusión deliberada de `branchNavigation.ts` (189 líneas, `ActiveBranchMap`/`getActiveChild`/`getCurrentLineNodes`):** resuelve "cuál es la rama activa" en un árbol CON variaciones; el alcance de Fase 3a es solo `tree.mainLine()`, así que no hace falta (hallazgo 16 del plan de Fase 3a) — se re-evalúa cuando una fase futura necesite reportes conscientes de variaciones. **Exclusión deliberada de `positionEval.ts`:** glue code impuro sobre el store propio de web-katrain (llama a un cliente de motor remoto propio de esa app); tengen ya tiene el equivalente vía `Analysis.scoreLead`/`winrate` de `@tengen/engine` (hallazgo 15). Ninguno de los dos es importado por este archivo en el original. tsc: 0→0 errores. | 4 | Si upstream cambia `getPlayedMoveQuality`/`getNextMoveQuality`, re-aplicar (1)/(2). Si upstream añade una nueva dependencia de `branchNavigation.ts`/`positionEval.ts` a este archivo, evaluar si el alcance de la fase vigente en ese momento ya cubre variaciones (si es así, portar `branchNavigation.ts` y revertir esta adaptación; si no, mantenerla). Gate: `apps/web/tests/playedMoveQuality.test.ts` (11 casos: raíz/move-null → null, mejor candidata → rank=1/"Best", override sin candidato, `formatBoardMoveLabel` pase/columna-salta-I/default, `getNextMoveQuality` sin hijos y con 2+ hijos verificando que el segundo NUNCA se usa). |
| `analyzeMcts.ts` | `src/engine/katago/analyzeMcts.ts` | (Task 8) **costura neuronal reemplazada.** (a) Quitado `import * as tf` y `import type { KataGoModelV8Tf } from './modelV8'` (modelV8/parseKataGoModelV8/binModelParser descartados enteros); quitado `import { POLICY_OPTIMISM, ROOT_POLICY_OPTIMISM } from './searchParams'` y `fillInputsV7Fast`/`computeLadderFeaturesV7KataGoInto`/`computeLadderedStonesV7KataGoInto`/`computeAreaMapV7KataGoInto` (muertos tras el rewire). **Nuevo edge de dependencia vendor→código de tengen:** `import { fillFeaturesV7NCHW } from '../../encoding/featuresV7'`, `type { GameState } from '../../encoding/gameState'`, `type { NNEvaluator } from '../../nn/evaluator'`. (b) Los tipos que upstream trae de `../../types` (`Player`/`GameRules`/`RegionOfInterest`/`FloatArray`/`BoardState`) y el `Move` local del vendor (`{x,y,player}`, ≠ `Move` público de tengen) → **defs locales** (mismo patrón que `featuresV7Fast.ts`); `getAnimationNow`/`ENGINE_MAX_*` (upstream `../../utils/animationFrame` y `./limits`, inexistentes en tengen) → **stubs inline** mínimos. (c) `evaluateBatch`: el build NHWC + `tf.tensor4d`/`model.forward()`/`.data()`/`.dispose()` + la mezcla policy-optimism (leía `policyArr[src+1]`) reemplazados por un puente por-estado `EvalState→GameState→fillFeaturesV7NCHW` (bin **NCHW** + global), transformación de simetría en NCHW, una sola `await evaluator.evaluate({bin,global,meta:null,batch,includeOwnership})` y consumo del `RawEval` **crudo** (policy head-0 pura; `postprocessKataGoV8` de Task 7 sin `postProcessParams` → defaults 20/20/20). (d) El handle `model: KataGoModelV8Tf` → `evaluator: NNEvaluator` en los 7 sitios + pasadas; `outputScaleMultiplier` = constante `1.0` (NNEvaluator no expone `postProcessParams`); `meta:null` con `// TODO(task-9)` para Human SL. (e) `tf.getBackend()==='webgpu'` (2 sitios del `analyzeMcts` standalone) → arg `preferLargeBatch` (default false). Limpieza acoplada: `getEvalScratch`/`EvalBatchScratch` sin scratch de área/escaleras/policy/pass, sin `EMPTY_AREA_MAP`/`hasLadderCandidates`/`PolicyValueOutput`/`hasOwnership`; `areaMap` fuera de `NeuralEval`; 6× `!` por `noUncheckedIndexedAccess` (lecturas en compound-assignment). Intacto: `expandNode` (softmax sobre legales), `selectEdge` (PUCT/FPU), virtual-loss (`inFlight`), backup, `getAnalysis`. tsc: **40→0**. (Task 7, passthrough **mantenido**) las llamadas a `getSqrtBoardArea(BOARD_AREA)`/`expectedWhiteScoreValue({..., boardSize: BOARD_SIZE})` siguen pasando los globals de `fastBoard.ts`; el MCTS sigue acoplado a un board size global (aceptable por la decisión "un Worker por tamaño", `decisiones-adaptacion.md §5`) — el desacople real NO era objetivo de Task 8. | 7, 8 | **El cambio de mayor riesgo de re-sync.** Localizado: en el diff de upstream, mirar si tocaron `evaluateBatch`/`expandNode`/`selectEdge`/`averageRootEvals`. Re-aplicar (a)–(e). El `bin` del root debe seguir saliendo de `fillFeaturesV7NCHW` (identidad) — el gate `tests/mcts.test.ts` (test 3, bin NCHW vs oráculo) lo caza. Gate: `mcts.test.ts` (mock determinista) + `test:nn`/`kata-raw-nn` (Task 10, correctitud real end-to-end). |

**Archivos 100% de tengen (no upstream, no re-sync — solo mantenimiento normal):** `types.ts`, `index.ts`, `f16.ts`, `encoding/gameState.ts`, `encoding/metaV1.ts`, `nn/session.ts`, `nn/evaluator.ts`, `search/mcts.ts`, `humansl.ts`, `engine.ts`, `worker/*`. (Nota: `encoding/featuresV7.ts` es un **fork** de `featuresV7Fast.ts`, no 100% tengen — está en la tabla de adaptaciones de arriba y lleva cabecera de origen.)

---

## Runbook de re-sync (cuando salga una release de web-katrain)

1. **Actualizar el pin y ver qué cambió upstream:**
   ```bash
   cd ~/dev/vendor/web-katrain && git fetch && git log --oneline 7a0a487..origin/main -- src/engine/katago/
   git diff 7a0a487..origin/main -- src/engine/katago/fastBoard.ts   # y por cada archivo de la tabla
   ```
2. **Por cada archivo de la tabla de arriba:** si upstream lo tocó, re-copiar la versión nueva a `packages/engine/src/vendor/web-katrain/`, re-poner la cabecera, y **re-aplicar los "Cambios de tengen"** de su fila (son pocos y localizados). Si upstream NO lo tocó, no hay nada que hacer.
   - Nota: `tests/board.test.ts` y `tests/ladderArea.test.ts` citan en comentarios números de línea aproximados (`~NNNN`) de `fastBoard.ts` como documentación (no se asertan); si `fastBoard.ts` cambia de tamaño en un re-sync, esos números derivan — actualizarlos si confunden, pero no bloquean. El test de Task 3 además depende del string literal `'Illegal suicide move'` (ver fila de `fastBoard.ts`).
3. **Actualizar el pin** en este documento (tabla de pins + cabeceras de los archivos) al nuevo commit.
4. **Correr los gates:**
   ```bash
   npm test -w @tengen/engine          # lógica (board, encoding diferencial, meta, MCTS mock)
   npm run -w @tengen/engine test:nn   # encoder+ONNX vs kata-raw-nn (correctitud real)
   npx -w @tengen/engine tsc --noEmit
   ```
   Si `test:nn` pasa, la adaptación es correcta aunque upstream haya reorganizado el código.
5. **Para releases de KataGo (red nueva o cambio de encoding):** regenerar los fixtures (`scripts/setup-katago.sh` + `gen-reference`) con la red nueva, y correr `test:nn`. Un cambio de `input version` de KataGo rompería el encoding → actualizar `encoding/featuresV7.ts` guiado por `nninputs.cpp` de la versión nueva (ver `fuentes.md §1`).

---

## Cómo retomar el proyecto (para un LLM sin contexto previo)

Leer en este orden:
1. **`CLAUDE.md`** (raíz) — qué es tengen, decisiones ya tomadas, estado actual.
2. **`docs/superpowers/specs/2026-07-08-tengen-design.md`** — la spec del producto.
3. **`docs/superpowers/plans/2026-07-09-fase-engine.md`** — el plan de implementación TDD (14 tasks + Task 0), con código concreto por paso.
4. **`docs/research/fase-engine/fuentes.md`** — datos duros verificados (encoding V7, MCTS, postproceso, contrato `kata-raw-nn`).
5. **`docs/research/fase-engine/decisiones-adaptacion.md`** — costuras de adaptación (NHWC→NCHW, policy head-0, inyección del evaluador, un Worker por tamaño) y oráculos/tolerancias de test.
6. **Este documento** — qué se adaptó de upstream y cómo re-aplicarlo.
7. **`.superpowers/sdd/progress.md`** (git-ignored) — el ledger: qué tasks están completos (con sus commits) y cuál es el siguiente. **Tras cualquier reinicio, confiar en el ledger + `git log` sobre la memoria.** Reanudar en el primer task no marcado como completo.

**Estado de ejecución y siguiente paso viven SOLO en el ledger** (`.superpowers/sdd/progress.md`); ningún otro documento debe hardcodear "qué task va" o "próxima acción" — se quedaría obsoleto y desviaría (esa duplicación ya causó una divergencia y se eliminó). La rama de trabajo es `fase-engine`.

**Cómo continuar la ejecución:** el plan se ejecuta con la skill `superpowers:subagent-driven-development` (un subagente implementador por task desde su brief `scripts/task-brief`, revisión por task con `scripts/review-package`, fixer para findings Critical/Important, ledger actualizado por task). Modelos: transcripción→`haiku`, integración→`sonnet`, adaptación de vendored grande / review final→el más capaz. Reanudar en el primer task del ledger no marcado como `complete`.
